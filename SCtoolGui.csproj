<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <ApplicationIcon>app.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <!-- WPF自動生成のMainではなく、Velopackフックを先に走らせるProgram.Mainを使う -->
    <StartupObject>SCtoolGui.Program</StartupObject>
    <!-- WPF標準Fluentテーマ(ThemeMode)は実験的APIのため、その診断を抑制する -->
    <NoWarn>$(NoWarn);WPF0001</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <Resource Include="app.ico" />
  </ItemGroup>

  <!-- ログ文言リソース。SDKが暗黙でEmbeddedResourceに含めるため Update で論理名だけ固定し、
       手書きアクセサ(LogMessages.cs)の ResourceManager から引く。 -->
  <ItemGroup>
    <EmbeddedResource Update="Resources\LogMessages.resx">
      <LogicalName>SCtoolGui.Resources.LogMessages.resources</LogicalName>
    </EmbeddedResource>
  </ItemGroup>

  <!-- App.xaml を ApplicationDefinition から Page に変更する。
       WPF に Main を自動生成させず、Program.Main を唯一のエントリポイントにするため。
       InitializeComponent は Page 扱いでも生成されるので App のリソース読込は従来どおり。 -->
  <ItemGroup>
    <ApplicationDefinition Remove="App.xaml" />
    <Page Include="App.xaml" />
  </ItemGroup>

  <!-- テストプロジェクトは独立してビルドするため、本体のコンパイル対象から除外する -->
  <ItemGroup>
    <Compile Remove="Tests\**" />
    <Page Remove="Tests\**" />
    <None Remove="Tests\**" />
  </ItemGroup>

  <ItemGroup>
    <None Update="shutter.mp3">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Velopack" Version="1.2.0" />
  </ItemGroup>

</Project>